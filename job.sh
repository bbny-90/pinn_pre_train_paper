# #!/bin/bash
# parent_path=$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )

# echo "$parent_path" 
# #cd "$parent_path"
# #cat ../some.text

#!/bin/bash
# Read a string with spaces using for loop
for value in dd ff gg
do
    echo $value
done